@model LoginViewModel
@{
    Layout = null;
    ViewData["Title"] = "Đăng nhập";
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Đăng nhập - VK Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            min-height: 100vh;
            display: grid;
            place-items: center;
            background:
                radial-gradient(circle at 18% 10%, rgba(96, 165, 250, 0.35), transparent 28rem),
                linear-gradient(135deg, #07225c 0%, #0b2f75 45%, #175cd3 100%);
            color: #0f172a;
            padding: 1rem;
        }

        .login-panel {
            width: min(440px, calc(100vw - 2rem));
            padding: 1.5rem;
            border: 1px solid rgba(219, 234, 254, 0.34);
            border-radius: 8px;
            background: #fff;
            box-shadow: 0 24px 70px rgba(0, 20, 73, 0.30);
        }

        .login-title {
            margin: 0;
            font-size: 1.75rem;
            font-weight: 800;
        }

        .login-note {
            margin-top: 0.45rem;
            color: #64748b;
        }

        .btn-primary {
            border-color: #175cd3;
            background: #175cd3;
            font-weight: 750;
        }

        .form-control {
            min-height: 44px;
            border-radius: 8px;
        }
    </style>
</head>
<body>
    <main class="login-panel">
        <h1 class="login-title">VK Admin</h1>
        <p class="login-note">Đăng nhập bằng tài khoản Admin hoặc Chủ nhà hàng.</p>

        @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
        {
            <div class="alert alert-danger">@Model.ErrorMessage</div>
        }

        <form asp-controller="Account" asp-action="Login" method="post">
            @Html.AntiForgeryToken()
            <input asp-for="ReturnUrl" type="hidden" />

            <div class="mb-3">
                <label asp-for="Username" class="form-label">Tài khoản</label>
                <input asp-for="Username" class="form-control" autocomplete="username" autofocus />
            </div>

            <div class="mb-3">
                <label asp-for="Password" class="form-label">Mật khẩu</label>
                <input asp-for="Password" class="form-control" type="password" autocomplete="current-password" />
            </div>

            <button type="submit" class="btn btn-primary w-100">Đăng nhập</button>
        </form>
    </main>
</body>
</html>
